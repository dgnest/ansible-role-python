---
# vars file for python (Ubuntu specific)

_python_pyenv_repo: https://github.com/yyuu/pyenv.git
_python_pyenv_virtualenv_repo: https://github.com/yyuu/pyenv-virtualenv.git

_python_path_file_environmentrc: "{{ ansible_env.HOME }}/.environmentrc"

_python:
  templates:
    - src: ".pyenvrc.j2"
      dest: "{{ python_pyenv_path }}/.pyenvrc"
      owner: "{{ python_pyenv_owner }}"
      group: "{{ python_pyenv_owner }}"
      mode: "0644"
    - src: ".environmentrc.j2"
      dest: "{{ _python_path_file_environmentrc }}"
      owner: "{{ python_pyenv_owner }}"
      group: "{{ python_pyenv_owner }}"
      mode: "0644"

_python_package_dependencies:
  - git
  - build-essential
  - libssl-dev
  - libbz2-dev
  - libreadline-dev
  - libsqlite3-dev
  - wget
  - curl
  - llvm
  - libtiff5-dev
  - libjpeg8-dev
  - zlib1g-dev
  - libfreetype6-dev
  - liblcms2-dev
  - libwebp-dev
  - tcl8.5-dev
  - tk8.5-dev
  - python-tk
